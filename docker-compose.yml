version: "3.9"

services:
  backend:
    build: .
    container_name: lensz-backend
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: unless-stopped
